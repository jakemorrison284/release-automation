name: Release
on:
  push:
    branches: [release/*]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: |
        VERSION=$(cat VERSION)
        git tag v$VERSION
        git push origin v$VERSION
